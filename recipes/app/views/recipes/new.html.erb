<h1>New Recipe</h1>

<%= form_for @recipe do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :mealtype %>
    <%= f.text_field :mealtype %>
    <%= f.label :user %>
    <%= f.collection_select :user, User.all, :id, :name %>
    <%= f.submit "Submit" %>
<% end %>